## 目标
- 将“运动、营养、睡眠、情绪”四个页面入口合并为一个“健康”主页面入口，底部导航只保留“首页｜数据｜健康｜AI助手｜我的”。
- 保留四个原页面作为“健康”页面内的子模块（tabs / 二级路由），实现统一视图与一致交互。

## 导航架构
- 新增 `BottomNavItem.Health(route = "health")`，替换原四个独立导航项。
- 保留深层路由：`exercise`, `nutrition`, `sleep`, `mental` 作为健康页内二级视图（tabs）或内部导航。
- 健康页采用 `NavHost` + `rememberSaveable` 或 `SegmentedTabs` 管理子模块状态；启用 `launchSingleTop` + `restoreState` 保持状态。

## 健康主页面设计（HealthHubScreen）
- Header：绿色/紫蓝渐变背景，展示总健康评分与今日建议；右上角“记录”按钮。
- 四模块入口方式（两种可选，默认Tabs）：
  1) 顶部分段标签：运动｜营养｜睡眠｜情绪（胶囊高亮、滚动切换、状态保持）
  2) 四宫格卡片入口：每个卡片显示关键指标与按钮（如“开始运动”“添加餐饮”“睡眠分析”“记录情绪”）
- 概览区：今日/本周关键数据汇总（运动分钟、热量摄入、睡眠时长、心情指数），统一卡片样式。
- 快速操作：横向按钮（运动、饮水、餐饮、体重、情绪记录），支持涟漪与缩放动效。

## 子模块内容
- 运动：保留现有页面内容，嵌入到健康页的子容器；按钮“开始”与推荐四宫格。
- 营养：卡路里进度条、餐饮记录、饮水格栅；拍照识别与手动添加在此展示。
- 睡眠：周趋势柱状图、分期环形图、统计条；评分徽章。
- 情绪：心情指数、8种情绪记录、趋势与统计；提示条。

## 组件与主题
- 统一卡片圆角与阴影（Shapes/Dimens已准备）；按钮与图标色随主题；进度条使用新API。
- 提取通用组件：统计卡、胶囊标签、徽章、进度条；复用于健康页和各子模块。

## 交互与动效
- `animateColorAsState`/`animateFloatAsState` 优化选中态与切换；`AnimatedVisibility` 控制内容切换。
- 快速操作与卡片点击提供涟漪与轻微缩放反馈；支持返回键正确行为（从子模块返回到健康概览）。

## 技术实现步骤
1) 添加 `HealthHubScreen`（Compose），集成顶部Header、分段标签/四宫格入口、概览与快速操作。
2) 将四个原页面作为 `HealthHubScreen` 子视图（tabs或嵌套导航），保留其现有功能。
3) 更新底部导航：替换四个入口为单一 `健康` 导航项；导航点击进入 `HealthHubScreen`，保持状态恢复。
4) 调整路由常量与图标资源，确保 IDE linter 无未解析引用。
5) 验证导航与状态：从健康页切换子模块、返回行为、重启恢复；构建与运行测试。

## 验证与交付
- 编译通过、IDE lint无误；底部导航中的“健康”可正常进入，子模块切换顺畅。
- 保留深层页面路由用于后续分享/跳转；统一UI风格。

请确认该方案，我将据此实施具体代码变更并进行验证。